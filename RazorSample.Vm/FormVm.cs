using System.Linq;

namespace RazorSample.Vm
{
  //public interface IFormVm<TCommand> : IVm, ICommandSource<TCommand> where TCommand : class { }

  //public sealed class FormVm<TCommand> : VmBase, IFormVm<TCommand> where TCommand : class
  //{
  //  public TCommand Command { get; internal set; }

  //  public Link OkAction => Actions.FirstOrDefault(action => action.Rel == RelTypes.Self);
  //  public Link CancelAction => Actions.FirstOrDefault(action => action.Rel == RelTypes.Prev);
  //}
}
